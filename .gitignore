# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
.eggs/

# Virtual environments
venv/
.venv/
env/

# Build / distribution
build/
dist/
*.whl
*.tar.gz

# Test + tool output
tfbs_results/
sample_analysis/tfbs_results/
*.log
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Editor / OS
.Rhistory
.DS_Store
.idea/
.vscode/

# Claude Code local state
.claude/

# HPC scratch (created by hpc/ scripts)
hpc/cache/
hpc/cas_scores/
hpc/artifacts/
hpc/transcript_lists/
hpc/ensembl_gtfs/
hpc/jaspar_2026/

# Experimental data downloaded at runtime by tfbs_footprinter3 -update.
# Only the small bundled JASPAR motif + nt-frequency files live in git;
# everything else under tfbs_footprinter3/data/ is fetched from S3 on first run
# and must NEVER be committed (can easily be >1 GB per species).
tfbs_footprinter3/data/*
!tfbs_footprinter3/data/pwms.json
!tfbs_footprinter3/data/JASPAR_2026_pwms.json
!tfbs_footprinter3/data/species_nt_freq.json
